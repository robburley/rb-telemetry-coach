interface:
  display_name: "Update Changelog"
  short_description: "Summarize unstaged changes and sync release versions"
  default_prompt: "Use $update-changelog to inspect unstaged changes, bump the package and extension manifest versions, update the README changelog, and provide a commit message."
